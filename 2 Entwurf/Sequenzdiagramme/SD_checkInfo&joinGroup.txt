@startuml
activate User
User-> GUI: ask for group info
activate GUI

GUI -> Server: askGroupInfo(currentGroupID)
activate Server

Server -> Database: getGroupInfo(groupID)
activate Database

Server <-- Database
deactivate Database

GUI <-- Server: showGroupInfo(currentGroupID)
deactivate Server
User<--GUI: 

User-> GUI: clicks on "join the group" button
GUI-> Server: askIfGroupFull(currentGroupID)
activate Server
Server -> Database: checkGroupFullness(GroupID)
activate Database
Server <-- Database
deactivate Database
GUI <-- Server
deactivate Server
alt group is not full
		GUI -> Server: joinGroup(currentGroupID)
		activate Server
		Server -> Database: addMember(groupID,userID)
	    activate Database
		Server <-- Database
		deactivate Database	
		GUI <-- Server
		deactivate Server	
		User<-GUI:show group page with confirmation

else group is full
		User<-GUI:"the chosen group is full"
		User<-GUI:show "choose one group" page
end
deactivate GUI
deactivate User


@enduml