
Dieses Klassendiagramm zeigt die Klassen der Schnittstelle des Servers. Diese Klassen stellen die Funktionalitäten für die Kommunikation zwischen Client und Server bereit.




Plant UML code:

@startuml

title Server Controller - Class Diagram


class UserController {
  +User getUserById(string userId)
  +User[] getAllUser()
  +void createUser(Credentials credentials)
  ..for admins..
  +void deleteUser(string userId)
}

class GroupController {
  +Group getGroupById(string groupId)
  +Group[] getGroupsByPrefferences(Prefferecnces prefs)
  +void createGroup(Group group)
  ..for admins..
  +void deleteGroup(string groupId)
}

class MembershipController {
  +boolean addUserToGroup(string userId, string groupId)
  +boolean removeUserFromGroup(string userId, string groupId)
}

class MensalineController {
  +Line[] getCurrentLineInfo()
}

class AuthenticationControler {
  +Token login(Credentials credentials)
}

class UserProfileController {
  +void updateUserProfile(string userId, User user)
}

@enduml